Route each support ticket using the captured local linear model. Escalate only
when the model score is strictly greater than zero; otherwise resolve it
automatically. Make exactly one routing tool call.
