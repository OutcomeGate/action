node_modules/
# Commit only the prebuilt runtime consumed by the Action and package. Test,
# demo, example, and source-map compiler output stays local.
dist/**
!dist/src/
!dist/src/**/
!dist/src/**/*.js
!dist/src/**/*.d.ts
.agentci/
.env
.env.*
!.env.example
runs/
traces/
coverage/
*.log
.DS_Store
.claude/settings.local.json

# Customer fixtures and raw traces must never be committed.
customer-data/
customer-fixtures/
